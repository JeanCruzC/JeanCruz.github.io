---
import Layout from '../layouts/Layout.astro';

const heroMetrics = [
  {
    value: '6+',
    label: 'años liderando WFM',
    description: 'Planificación multicanal en LATAM',
    gradient: 'from-indigo-500 via-sky-500 to-blue-500'
  },
  {
    value: '25+',
    label: 'proyectos de optimización',
    description: 'Automatizaciones, dashboards y modelos',
    gradient: 'from-emerald-500 via-teal-500 to-cyan-500'
  },
  {
    value: '98%',
    label: 'SLA de cumplimiento',
    description: 'Planes accionables basados en datos',
    gradient: 'from-purple-500 via-indigo-500 to-blue-500'
  }
];

const quickActions = [
  { href: '#dashboards', label: 'Dashboards', description: 'Looker Studio · Power BI', icon: '📊' },
  { href: '#proyectos', label: 'Automatizaciones', description: 'Streamlit · Python · RPA', icon: '🤖' },
  { href: '#servicios', label: 'Servicios', description: 'Consultoría & workshops', icon: '🛠️' },
  { href: '#contacto', label: 'Agenda una reunión', description: 'Calendly · LinkedIn', icon: '📅' }
];

const focusAreas = ['Workforce Intelligence', 'Forecasting', 'Automatización', 'CX Analytics'];

const dashboards = [
  {
    tag: 'Looker Studio',
    title: 'Reporte comercial en tiempo real',
    description:
      'KPIs ejecutivos para monitorear revenue, NPS y productividad con actualizaciones cada 15 minutos.',
    cta: 'Solicitar demo personalizada'
  },
  {
    tag: 'Power BI',
    title: 'Suite de experiencia del cliente',
    description:
      'Tableros que conectan SLAs, experiencia digital y costos operativos para decisiones estratégicas.',
    cta: 'Ver tablero interactivo'
  },
  {
    tag: 'Google Sheets',
    title: 'Control de operaciones diario',
    description:
      'Plantillas colaborativas con indicadores críticos y alertas automáticas para líderes de servicio.',
    cta: 'Explorar plantilla viva'
  },
  {
    tag: 'Excel · VBA',
    title: 'Modelo de escenarios para staffing',
    description: 'Simulaciones what-if para ajustar headcount, horarios y presupuesto en segundos.',
    cta: 'Probar el modelo'
  }
];

const services = [
  {
    title: 'Implementación end-to-end de dashboards',
    description:
      'Desde la arquitectura de datos hasta la adopción ejecutiva con tableros que responden en segundos.',
    deliverables: ['Workshop de descubrimiento', 'Modelado semántico', 'Entrenamiento de usuarios']
  },
  {
    title: 'Automatización de reportes y alertas',
    description:
      'Pipelines de datos, bots y notificaciones proactivas que liberan horas del equipo cada semana.',
    deliverables: ['Data pipelines', 'Bots en Python', 'Alertas en tiempo real']
  },
  {
    title: 'Mentoría y capacitación en Workforce Analytics',
    description:
      'Sesiones a medida para equipos de planificación, forecasting y command center.',
    deliverables: ['Planes de madurez', 'Playbooks operativos', 'Capacitación in-company']
  }
];

const cvHighlights = [
  'Implementación regional de Workforce Management para operaciones multicanal.',
  'Forecasting y dimensionamiento que optimizan cobertura y reducen costos operativos.',
  'Automatización de reportes con Python, SQL y Power BI reduciendo tiempos de análisis en 60%.',
  'Liderazgo de equipos de hasta 220 asesores alineando metas de CX y productividad.',
  'Experiencia en IEX, Verint, Genesys Cloud, NICE y Power Automate.'
];

const projects = [
  {
    title: 'Workforce AI Forecaster',
    category: 'AI/ML',
    year: 2024,
    status: 'Completed',
    featured: true,
    duration: '3 meses',
    client: 'Concentrix LATAM',
    summary:
      'Modelo predictivo que integra señales de demanda, ausentismo y estacionalidad para ajustar planes de staffing en minutos.',
    stack: ['Python', 'Prophet', 'Power BI', 'Azure ML'],
    ctas: [
      { label: 'Project Details', href: '#', external: false },
      { label: 'View Code', href: 'https://github.com/', external: true }
    ],
    accent: 'from-indigo-500 via-sky-500 to-emerald-400'
  },
  {
    title: 'Command Center Automation Suite',
    category: 'Automatización',
    year: 2023,
    status: 'Completed',
    featured: false,
    duration: '8 semanas',
    client: 'Telecom BPO',
    summary:
      'Bots que consolidan KPIs de tiempo real, generan reportes accionables y envían alertas proactivas a supervisores.',
    stack: ['Python', 'Airflow', 'Slack API', 'BigQuery'],
    ctas: [{ label: 'Project Details', href: '#', external: false }],
    accent: 'from-fuchsia-500 via-purple-500 to-indigo-500'
  },
  {
    title: 'CX Executive Dashboard Suite',
    category: 'Dashboards',
    year: 2024,
    status: 'En curso',
    featured: true,
    duration: '6 semanas',
    client: 'Operador multicanal',
    summary:
      'Tableros en Power BI que conectan satisfacción, desempeño operativo y costos para directores regionales.',
    stack: ['Power BI', 'DAX', 'Azure Synapse'],
    ctas: [{ label: 'Live Demo', href: '#', external: true }],
    accent: 'from-amber-500 via-orange-500 to-rose-500'
  },
  {
    title: 'Scheduling Scenario Planner',
    category: 'Operaciones',
    year: 2022,
    status: 'Completed',
    featured: false,
    duration: '4 semanas',
    client: 'Centro de contacto regional',
    summary:
      'Herramienta colaborativa que simula escenarios what-if y genera recomendaciones para staffing y budget.',
    stack: ['Google Sheets', 'Apps Script', 'Looker Studio'],
    ctas: [{ label: 'View Workbook', href: '#', external: true }],
    accent: 'from-emerald-500 via-teal-400 to-cyan-400'
  },
  {
    title: 'Voice of Agent Insights Lab',
    category: 'Analytics',
    year: 2023,
    status: 'Completed',
    featured: false,
    duration: '5 semanas',
    client: 'Concentrix Insights',
    summary:
      'Pipeline de NLP que clasifica feedback de agentes y anticipa riesgos de rotación con dashboards ejecutivos.',
    stack: ['Python', 'spaCy', 'Looker Studio'],
    ctas: [{ label: 'View Notebook', href: '#', external: true }],
    accent: 'from-blue-500 via-indigo-500 to-purple-500'
  },
  {
    title: 'Global Forecast Governance Playbook',
    category: 'Operaciones',
    year: 2021,
    status: 'Completed',
    featured: false,
    duration: '3 meses',
    client: 'Región Norteamérica',
    summary:
      'Framework de gobernanza que estandariza procesos de forecast y reportes en cinco países.',
    stack: ['Miro', 'Notion', 'Power Automate'],
    ctas: [{ label: 'Project Details', href: '#', external: false }],
    accent: 'from-slate-500 via-slate-600 to-slate-700'
  }
];

const categoryCounts = projects.reduce((acc, project) => {
  acc[project.category] = (acc[project.category] ?? 0) + 1;
  return acc;
}, {} as Record<string, number>);

const projectFilters = [
  { label: `Todos · ${projects.length}`, value: 'all', count: projects.length },
  ...Object.entries(categoryCounts).map(([category, count]) => ({
    label: `${category} · ${count}`,
    value: category,
    count
  }))
];

const projectSummary = `${projects.length} proyectos · ${categoryCounts['AI/ML'] ?? 0} IA/ML · ${
  categoryCounts['Automatización'] ?? 0
} automatización · ${categoryCounts['Dashboards'] ?? 0} dashboards ejecutivos`;
---
<Layout
  title="Jean Cruz | Portafolio de Analítica y Datos"
  description="Explora dashboards interactivos, proyectos de automatización y experiencias de Workforce Management lideradas por Jean Cruz."
>
  <div class="max-w-6xl mx-auto px-6 pb-20 space-y-24 md:space-y-28">
    <section
      id="inicio"
      data-section
      class="relative overflow-hidden rounded-[42px] bg-gradient-to-br from-slate-950 via-slate-900 to-slate-950 px-8 py-12 md:px-16 md:py-16 text-slate-100 shadow-[0_40px_120px_-45px_rgba(15,23,42,0.9)]"
    >
      <div class="absolute -top-32 left-10 h-72 w-72 rounded-full bg-indigo-500/30 blur-[110px]"></div>
      <div class="absolute -bottom-28 right-6 h-64 w-64 rounded-full bg-emerald-400/20 blur-[100px]"></div>
      <div class="relative grid gap-12 lg:grid-cols-[1.2fr_0.95fr] items-start">
        <div class="space-y-10">
          <div class="inline-flex items-center gap-3 rounded-full border border-white/10 bg-white/5 px-5 py-2 text-[0.7rem] font-semibold uppercase tracking-[0.35em] text-slate-200">
            Portafolio Profesional
          </div>
          <div class="space-y-4">
            <p class="text-sm font-semibold uppercase tracking-[0.28em] text-indigo-200">Jean Cruz · Lima, Perú</p>
            <h1 class="text-4xl font-semibold leading-[1.05] md:text-5xl lg:text-[3.35rem]">
              Diseñador de operaciones basado en datos para experiencias memorables
            </h1>
            <p class="max-w-2xl text-lg leading-relaxed text-slate-200/80 md:text-xl">
              Transformo insights en planes de staffing, automatizaciones y tableros ejecutivos que elevan la experiencia del
              cliente y el rendimiento operativo. Disponibilidad para proyectos remotos e híbridos en LATAM y Norteamérica.
            </p>
          </div>
          <div class="flex flex-col gap-6 md:flex-row md:items-center">
            <div class="flex flex-wrap gap-3">
              <a
                href="#cv"
                data-scroll-to="#cv"
                class="inline-flex items-center gap-2 rounded-full bg-white text-slate-950 px-6 py-3 text-sm font-semibold shadow-[0_20px_50px_rgba(255,255,255,0.12)] transition hover:-translate-y-1 hover:bg-slate-100"
              >
                Descargar CV
                <span aria-hidden="true">↗</span>
              </a>
              <a
                href="#contacto"
                data-scroll-to="#contacto"
                class="inline-flex items-center gap-2 rounded-full border border-white/30 bg-white/10 px-6 py-3 text-sm font-semibold text-white transition hover:-translate-y-1 hover:bg-white/20"
              >
                Agenda una reunión
              </a>
            </div>
            <div class="flex flex-wrap items-center gap-2 text-xs font-semibold uppercase tracking-[0.28em] text-slate-300">
              <span class="text-slate-200/80">Ir directo a</span>
              {quickActions.map((action) => (
                <a
                  href={action.href}
                  data-scroll-to={action.href}
                  class="group inline-flex items-center gap-1 rounded-full border border-white/20 bg-white/5 px-3 py-1.5 text-[0.65rem] tracking-widest transition hover:border-white/40 hover:bg-white/20"
                >
                  <span class="size-1.5 rounded-full bg-white/30 transition group-hover:bg-white"></span>
                  {action.label}
                </a>
              ))}
            </div>
          </div>
          <div class="grid gap-4 sm:grid-cols-3">
            {heroMetrics.map((metric) => (
              <div class={`rounded-2xl p-[1px] bg-gradient-to-br ${metric.gradient}`}>
                <div class="rounded-2xl bg-slate-950/75 p-5 shadow-[0_15px_35px_rgba(15,23,42,0.35)]">
                  <p class="text-3xl font-semibold text-white">{metric.value}</p>
                  <p class="mt-1 text-xs font-semibold uppercase tracking-widest text-slate-300">{metric.label}</p>
                  <p class="mt-3 text-sm text-slate-200/80">{metric.description}</p>
                </div>
              </div>
            ))}
          </div>
          <div class="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">
            {quickActions.map((action) => (
              <a
                href={action.href}
                data-scroll-to={action.href}
                class="group flex items-start gap-3 rounded-2xl border border-white/10 bg-white/10 p-4 transition hover:-translate-y-1 hover:border-white/30 hover:bg-white/20"
              >
                <span class="text-xl leading-none">{action.icon}</span>
                <div class="flex-1">
                  <p class="text-sm font-semibold text-white">{action.label}</p>
                  <p class="text-[0.78rem] leading-relaxed text-slate-200/75">{action.description}</p>
                </div>
                <span aria-hidden="true" class="mt-1 text-sm text-white/60 transition group-hover:text-white">↗</span>
              </a>
            ))}
          </div>
        </div>
        <div class="space-y-6">
          <div class="rounded-3xl border border-white/10 bg-white/10 p-8 shadow-[0_25px_60px_-35px_rgba(15,23,42,0.9)]">
            <div class="flex items-start justify-between gap-4">
              <div>
                <p class="text-xs font-semibold uppercase tracking-[0.28em] text-slate-200/80">Disponibilidad</p>
                <p class="mt-2 text-lg font-semibold text-white">Consultorías y proyectos 2024</p>
              </div>
              <span class="rounded-full bg-emerald-400/20 px-4 py-1 text-xs font-semibold text-emerald-200">Abierto a propuestas</span>
            </div>
            <div class="mt-6 space-y-3 text-sm text-slate-200/80">
              <p>
                <strong class="text-white">Industria:</strong> BPO · Telecomunicaciones · Experiencia del cliente
              </p>
              <p>
                <strong class="text-white">Ubicación:</strong> Lima, Perú (GMT-5) · Trabajo remoto/híbrido
              </p>
            </div>
            <div class="mt-6 flex flex-wrap gap-2 text-[0.7rem] font-semibold uppercase tracking-[0.25em] text-slate-200/70">
              {focusAreas.map((area) => (
                <span class="rounded-full bg-white/5 px-4 py-2">{area}</span>
              ))}
            </div>
          </div>
          <div class="rounded-3xl border border-white/10 bg-white/5 p-6">
            <p class="text-sm font-semibold uppercase tracking-[0.3em] text-indigo-200">Conectemos</p>
            <div class="mt-4 space-y-3 text-sm text-slate-200/80">
              <a
                href="mailto:jeancruzc@gmail.com"
                class="group flex items-center justify-between gap-4 rounded-2xl border border-white/0 bg-white/0 px-4 py-3 transition hover:border-white/20 hover:bg-white/10"
              >
                <span>jeancruzc@gmail.com</span>
                <span aria-hidden="true" class="text-white/60 transition group-hover:text-white">↗</span>
              </a>
              <a
                href="https://www.linkedin.com"
                target="_blank"
                rel="noopener"
                class="group flex items-center justify-between gap-4 rounded-2xl border border-white/0 bg-white/0 px-4 py-3 transition hover:border-white/20 hover:bg-white/10"
              >
                <span>LinkedIn</span>
                <span aria-hidden="true" class="text-white/60 transition group-hover:text-white">↗</span>
              </a>
              <a
                href="https://github.com"
                target="_blank"
                rel="noopener"
                class="group flex items-center justify-between gap-4 rounded-2xl border border-white/0 bg-white/0 px-4 py-3 transition hover:border-white/20 hover:bg-white/10"
              >
                <span>GitHub</span>
                <span aria-hidden="true" class="text-white/60 transition group-hover:text-white">↗</span>
              </a>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section id="sobre-mi" data-section class="grid gap-10 lg:grid-cols-[1.15fr_0.95fr] items-start">
      <div class="space-y-6">
        <p class="uppercase tracking-[0.25em] text-xs text-indigo-500 font-semibold">Sobre mí</p>
        <h2 class="text-3xl md:text-4xl font-semibold text-slate-900">Presentación personal</h2>
        <div class="space-y-4 text-base text-slate-600 leading-relaxed">
          <p>
            Soy Workforce Manager con trayectoria en BPO, telecomunicaciones y servicios financieros liderando equipos de
            planeamiento, control en vivo y analítica operativa. Coordino estrategias de cobertura multicanal integrando datos
            y ejecución ágil.
          </p>
          <p>
            He implementado modelos de forecasting regionales, automatizado reportes con Python y SQL, y alineado objetivos
            comerciales con indicadores de experiencia del cliente para impulsar decisiones oportunas.
          </p>
          <p>
            Busco colaborar con organizaciones que deseen escalar su capacidad analítica, mejorar la productividad y diseñar
            operaciones centradas en el cliente a través de datos confiables y accionables.
          </p>
        </div>
      </div>
      <div class="glass rounded-3xl p-8 space-y-6 border border-white/60 shadow-xl">
        <h3 class="text-xl font-semibold text-slate-900">Highlight profesional</h3>
        <div class="space-y-3 text-sm text-slate-600">
          <p><strong class="text-slate-900">Rol actual:</strong> Workforce Manager en Concentrix.</p>
          <p><strong class="text-slate-900">Industria:</strong> BPO · Experiencia del cliente · Telecomunicaciones</p>
          <p><strong class="text-slate-900">Ubicación:</strong> Lima, Perú (remoto/híbrido)</p>
        </div>
        <div class="grid grid-cols-2 gap-3 text-xs font-medium text-slate-500 uppercase tracking-widest">
          <span class="rounded-full bg-slate-900/5 px-4 py-2 text-center">Workforce Management</span>
          <span class="rounded-full bg-slate-900/5 px-4 py-2 text-center">Forecasting</span>
          <span class="rounded-full bg-slate-900/5 px-4 py-2 text-center">Data Analytics</span>
          <span class="rounded-full bg-slate-900/5 px-4 py-2 text-center">Automatización</span>
        </div>
      </div>
    </section>

    <section id="dashboards" data-section class="space-y-12">
      <div class="flex flex-col gap-6 md:flex-row md:items-end md:justify-between">
        <div class="space-y-4 max-w-2xl">
          <p class="uppercase tracking-[0.25em] text-xs text-indigo-500 font-semibold">Dashboards &amp; Reportes</p>
          <h2 class="text-3xl md:text-4xl font-semibold text-slate-900">Galería de tableros interactivos</h2>
          <p class="text-base text-slate-600 leading-relaxed">
            Dashboards creados para acelerar decisiones ejecutivas con contexto, storytelling y foco en métricas accionables.
          </p>
        </div>
        <a
          href="#contacto"
          data-scroll-to="#contacto"
          class="text-sm font-semibold text-indigo-600 hover:text-indigo-700"
        >
          Solicitar una demo personalizada →
        </a>
      </div>
      <div class="grid gap-8 md:grid-cols-2 xl:grid-cols-2">
        {dashboards.map((item) => (
          <article class="glass rounded-3xl p-8 space-y-4 border border-white/60 shadow-lg flex flex-col justify-between">
            <div class="space-y-3">
              <span class="inline-flex items-center gap-2 text-xs font-semibold uppercase tracking-widest text-indigo-500">
                <span class="size-2 rounded-full bg-indigo-500"></span>
                {item.tag}
              </span>
              <h3 class="text-2xl font-semibold text-slate-900">{item.title}</h3>
              <p class="text-sm text-slate-600 leading-relaxed">{item.description}</p>
            </div>
            <a
              href="#contacto"
              data-scroll-to="#contacto"
              class="inline-flex items-center justify-between gap-2 rounded-2xl border border-indigo-100 bg-white/80 px-4 py-3 text-xs font-semibold text-indigo-600 transition hover:border-indigo-400 hover:text-indigo-700"
            >
              {item.cta}
              <span aria-hidden="true">↗</span>
            </a>
          </article>
        ))}
      </div>
    </section>

    <section id="proyectos" data-section class="space-y-12">
      <div class="space-y-4 max-w-3xl">
        <p class="uppercase tracking-[0.25em] text-xs text-indigo-500 font-semibold">Aplicaciones interactivas</p>
        <h2 class="text-3xl md:text-4xl font-semibold text-slate-900">Proyectos creados con datos, automatización e IA</h2>
        <p class="text-base text-slate-600 leading-relaxed">
          {projectSummary}
        </p>
      </div>
      <div class="flex flex-wrap items-center gap-3 text-sm">
        {projectFilters.map((filter, index) => (
          <button
            type="button"
            class={`filter-chip rounded-full border px-4 py-2 transition ${index === 0 ? 'is-active' : ''}`}
            data-filter-button={filter.value}
            aria-pressed={index === 0 ? 'true' : 'false'}
          >
            {filter.label}
          </button>
        ))}
      </div>
      <div class="grid gap-8 md:grid-cols-2 xl:grid-cols-3" id="projects-grid">
        {projects.map((project) => (
          <article
            class="project-card group flex h-full flex-col gap-4 rounded-3xl border border-slate-200/70 bg-white/80 p-6 shadow-[0_30px_60px_-40px_rgba(15,23,42,0.35)] transition hover:-translate-y-1 hover:shadow-[0_40px_90px_-45px_rgba(15,23,42,0.45)]"
            data-category={project.category}
            data-project-card
          >
            <div class={`rounded-2xl bg-gradient-to-r ${project.accent} p-1 text-xs font-semibold uppercase tracking-[0.28em] text-white`}> 
              <div class="flex items-center gap-2 rounded-2xl bg-white/90 px-4 py-2 text-slate-700">
                <span>{project.category}</span>
                <span class="text-[0.65rem] text-slate-500">{project.status}</span>
                {project.featured && <span class="ml-auto rounded-full bg-slate-900 px-3 py-1 text-[0.6rem] text-white">Featured</span>}
                <span class="ml-auto text-[0.7rem] text-slate-500">{project.year}</span>
              </div>
            </div>
            <div class="space-y-3">
              <h3 class="text-xl font-semibold text-slate-900">
                <a href="#" class="group-hover:text-indigo-600 transition">{project.title}</a>
              </h3>
              <p class="text-sm text-slate-600 leading-relaxed">{project.summary}</p>
            </div>
            <div class="text-xs text-slate-500">
              {project.duration} · Cliente: <span class="font-semibold text-slate-700">{project.client}</span>
            </div>
            <div class="flex flex-wrap gap-2 text-xs font-semibold text-indigo-600 uppercase tracking-widest">
              {project.stack.map((tech) => (
                <span class="rounded-full bg-indigo-50 text-indigo-600 px-3 py-1">{tech}</span>
              ))}
            </div>
            <div class="mt-auto flex flex-wrap gap-2 pt-2">
              {project.ctas.map((cta) => (
                <a
                  href={cta.href}
                  target={cta.external ? '_blank' : undefined}
                  rel={cta.external ? 'noopener' : undefined}
                  class="inline-flex items-center gap-2 rounded-2xl border border-slate-200 bg-white/80 px-4 py-2 text-xs font-semibold text-slate-700 transition hover:border-indigo-400 hover:text-indigo-600"
                >
                  {cta.label}
                  <span aria-hidden="true">↗</span>
                </a>
              ))}
            </div>
          </article>
        ))}
      </div>
    </section>

    <section id="servicios" data-section class="glass rounded-[32px] border border-white/60 shadow-xl p-10 md:p-14 space-y-10">
      <div class="space-y-4 max-w-3xl">
        <p class="uppercase tracking-[0.25em] text-xs text-indigo-500 font-semibold">Cómo puedo ayudarte</p>
        <h2 class="text-3xl md:text-4xl font-semibold text-slate-900">Servicios y propuestas de colaboración</h2>
        <p class="text-base text-slate-600 leading-relaxed">
          Diseñamos soluciones llave en mano para líderes de operaciones y CX que requieren decisiones basadas en datos y equipos
          empoderados.
        </p>
      </div>
      <div class="grid gap-8 md:grid-cols-3">
        {services.map((service) => (
          <div class="rounded-3xl bg-white/80 border border-slate-200/80 p-8 space-y-5 shadow-[0_25px_60px_-45px_rgba(15,23,42,0.4)]">
            <div class="inline-flex size-12 items-center justify-center rounded-full bg-gradient-to-br from-indigo-500/90 to-blue-500/90 text-white text-xl font-semibold">
              •
            </div>
            <p class="text-base font-medium text-slate-900">{service.title}</p>
            <p class="text-sm text-slate-500 leading-relaxed">{service.description}</p>
            <ul class="space-y-2 text-xs text-slate-500">
              {service.deliverables.map((deliverable) => (
                <li class="flex items-center gap-2">
                  <span class="size-1.5 rounded-full bg-indigo-400"></span>
                  {deliverable}
                </li>
              ))}
            </ul>
          </div>
        ))}
      </div>
    </section>

    <section id="cv" data-section class="grid gap-10 lg:grid-cols-[1fr_1fr] items-center">
      <div class="space-y-6">
        <p class="uppercase tracking-[0.25em] text-xs text-indigo-500 font-semibold">Currículum Vitae</p>
        <h2 class="text-3xl md:text-4xl font-semibold text-slate-900">Comparte tu trayectoria en detalle</h2>
        <p class="text-base text-slate-600 leading-relaxed">
          Conoce el detalle de mi experiencia coordinando operaciones de workforce, automatizando reportes y liderando equipos
          multidisciplinarios.
        </p>
        <div class="flex flex-wrap gap-4">
          <a
            href="#"
            class="inline-flex items-center gap-2 rounded-full bg-indigo-600 text-white px-6 py-3 text-sm font-semibold shadow-lg shadow-indigo-500/30 transition hover:-translate-y-1 hover:bg-indigo-700"
          >
            Descargar CV en PDF
          </a>
          <a
            href="/cv"
            class="inline-flex items-center gap-2 rounded-full border border-indigo-200 bg-white/70 px-6 py-3 text-sm font-semibold text-indigo-600 transition hover:-translate-y-1 hover:border-indigo-400 hover:text-indigo-700"
          >
            Ver CV versión web
          </a>
        </div>
      </div>
      <div class="glass rounded-3xl p-8 space-y-4 border border-white/60 shadow-xl">
        <h3 class="text-xl font-semibold text-slate-900">Highlights del CV</h3>
        <ul class="space-y-3 text-sm text-slate-600 leading-relaxed">
          {cvHighlights.map((item) => (
            <li>• {item}</li>
          ))}
        </ul>
      </div>
    </section>

    <section id="contacto" data-section class="space-y-8">
      <div class="glass rounded-3xl p-10 md:p-14 border border-white/60 shadow-xl">
        <div class="space-y-6 text-center max-w-3xl mx-auto">
          <p class="uppercase tracking-[0.25em] text-xs text-indigo-500 font-semibold">Contacto</p>
          <h2 class="text-3xl md:text-4xl font-semibold text-slate-900">¿Listo para llevar tu operación al siguiente nivel?</h2>
          <p class="text-base text-slate-600 leading-relaxed">
            Cuéntame sobre tus retos de planificación, automatización o analítica y diseñemos juntos una hoja de ruta. Agenda una
            llamada o escríbeme directamente.
          </p>
          <div class="flex flex-wrap justify-center gap-4">
            <a
              href="mailto:jeancruzc@gmail.com"
              class="inline-flex items-center gap-2 rounded-full bg-slate-900 text-white px-6 py-3 text-sm font-semibold shadow-lg shadow-slate-900/20 transition hover:-translate-y-1 hover:bg-indigo-600"
            >
              Enviar correo
            </a>
            <a
              href="https://www.linkedin.com"
              target="_blank"
              rel="noopener"
              class="inline-flex items-center gap-2 rounded-full border border-slate-900/10 bg-white/70 px-6 py-3 text-sm font-semibold text-slate-900 transition hover:-translate-y-1 hover:border-indigo-500/60 hover:text-indigo-600"
            >
              Conectar en LinkedIn
            </a>
          </div>
        </div>
      </div>
      <section class="rounded-3xl border border-slate-200 bg-white/70 p-8 text-center">
        <h3 class="text-2xl font-semibold text-slate-900 mb-2">¿Listo para iniciar tu proyecto?</h3>
        <p class="text-slate-600 mb-4">
          Hagamos una sesión de descubrimiento para identificar oportunidades de impacto inmediato.
        </p>
        <div class="flex flex-wrap justify-center gap-3">
          <a
            href="#contacto"
            data-scroll-to="#contacto"
            class="inline-flex items-center gap-2 rounded-full bg-indigo-600 text-white px-5 py-2.5 text-sm font-semibold transition hover:-translate-y-1 hover:bg-indigo-700"
          >
            Start a Conversation
          </a>
          <a
            href="#cv"
            data-scroll-to="#cv"
            class="inline-flex items-center gap-2 rounded-full border border-indigo-200 bg-white/80 px-5 py-2.5 text-sm font-semibold text-indigo-600 transition hover:-translate-y-1 hover:border-indigo-400 hover:text-indigo-700"
          >
            Download Resume
          </a>
        </div>
      </section>
      <p class="text-center text-xs text-slate-500">© {new Date().getFullYear()} Jean Cruz · Diseñado para destacar tu talento en datos.</p>
    </section>
  </div>

  <script>
    const filterButtons = document.querySelectorAll('[data-filter-button]');
    const projectCards = document.querySelectorAll('[data-project-card]');
    filterButtons.forEach((button) => {
      button.addEventListener('click', () => {
        const filter = button.getAttribute('data-filter-button');
        filterButtons.forEach((btn) => {
          const isActive = btn === button;
          btn.classList.toggle('is-active', isActive);
          btn.setAttribute('aria-pressed', isActive.toString());
        });
        projectCards.forEach((card) => {
          const match = filter === 'all' || card.getAttribute('data-category') === filter;
          card.classList.toggle('hidden', !match);
          card.classList.toggle('opacity-0', !match);
          card.classList.toggle('-translate-y-3', !match);
        });
      });
    });

    const scrollLinks = document.querySelectorAll('[data-scroll-to]');
    scrollLinks.forEach((link) => {
      link.addEventListener('click', (event) => {
        const targetSelector = link.getAttribute('data-scroll-to');
        if (!targetSelector) return;
        const target = document.querySelector(targetSelector);
        if (!target) return;
        event.preventDefault();
        target.scrollIntoView({ behavior: 'smooth', block: 'start' });
        target.classList.add('section-highlight');
        setTimeout(() => target.classList.remove('section-highlight'), 1400);
        history.replaceState(null, '', targetSelector);
      });
    });
  </script>

  <style lang="css">
    :global(.filter-chip) {
      border-color: rgba(148, 163, 184, 0.35);
      color: rgb(71, 85, 105);
      background: rgba(255, 255, 255, 0.8);
    }

    :global(.filter-chip.is-active) {
      background: rgb(15, 23, 42);
      color: white;
      border-color: rgba(15, 23, 42, 0.95);
      box-shadow: 0 12px 30px rgba(15, 23, 42, 0.35);
    }

    :global(.project-card) {
      transition: transform 250ms ease, opacity 200ms ease;
    }

    :global(.section-highlight) {
      animation: sectionPulse 1.1s ease-out;
    }

    @keyframes sectionPulse {
      0% {
        box-shadow: 0 0 0 rgba(79, 70, 229, 0);
      }
      35% {
        box-shadow: 0 0 0 14px rgba(79, 70, 229, 0.15);
      }
      100% {
        box-shadow: 0 0 0 0 rgba(79, 70, 229, 0);
      }
    }
  </style>
</Layout>
